import { TaskSortingOrderPipe } from './task-sorting-order.pipe';

describe('TaskSortingOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskSortingOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
